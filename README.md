# ML-PRACS

GENERAL INSTRUCTIONS:

Files with name as ML_exptname can be directly opened in google colab, eg. DAV_Linear
Files with name as just exptname contains only the code which is seperated by leaving lines in between indicating different cells which can be run on google colab, eg. LINEAR

INSTRUCTIONS FOR CERTAIN EXPTS:

FOR HEBBIAN LEARNING EXPT:
If OR function is asked then just replace the y value for which refer to the note at the end of HEBBIAN file

FOR MCCULLOCH PITTS:
Files related to MCP have all threw gates AND, OR and NAND 
If only AND and OR is asked just delete the cells which contains code for NAND (only two cells are present, one which defines y_nand and the last cell)
Similarly, if OR and NAND is asked then drop the AND cells

FOR ALL EXPTS:
There are expts where it feels like extra implementations have been done mainly related to data visualizations like in LINEAR REGRESSION expt there is boxplot, histogram, heatmap and all which is implemented so then for such type of experiments just delete that particular cell which contains code for a particular data visualization.
